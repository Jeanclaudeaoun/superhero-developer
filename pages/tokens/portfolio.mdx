---
title: "Portfolio & PnL"
description: "Track what you hold, what it is actually worth on exit, and what you have made or lost."
---

# Portfolio & PnL

Superhero keeps two separate views of a position: the **holdings** list, which is your current
balances priced at spot, and the **portfolio history**, which is a series of valuation snapshots with
profit and loss attached. Both are addressable for any account, not just your own. This page
documents the exact field names — agents parse them — and explains why a position's quoted value is
systematically higher than what you would get for selling it.

## From the command line

```bash
node scripts/superhero-portfolio.mjs holdings          # your positions
node scripts/superhero-portfolio.mjs holdings ak_...   # anyone's positions
node scripts/superhero-portfolio.mjs holdings ak_... 20 2   # limit 20, page 2
node scripts/superhero-portfolio.mjs summary           # holdings + latest snapshot
node scripts/superhero-portfolio.mjs history           # value over time
```

`holdings` and `summary` read the public API and need no signing key for another account's address —
but the script derives *your* address from `AE_PRIVATE_KEY`, so the no-argument forms require it to
be set.

## Holdings

Backed by `GET /api/accounts/{address}/tokens`, with `includes=token` so each row carries the full
token record.

| Field in the script's output | Source | Meaning |
| --- | --- | --- |
| `token_name` | `token.name` | The token's name, which is also its symbol |
| `token_symbol` | `token.symbol` | Same string in practice |
| `sale_address` | `token.sale_address` | The bonding-curve contract — the handle for buy and sell |
| `token_address` | `aex9_address` or `token.address` | The AEX-9 contract, for allowances and transfers |
| `balance` | `balance` | Tokens held |
| `buy_price_ae` | `token.price` | Current spot **buy** price, per token |
| `sell_price_ae` | `token.sell_price` | Current spot **sell** price, per token — 0.5% below buy |
| `trending_score` | `token.trending_score` | Momentum signal for that token |

Value a position at `sell_price_ae`, never at `buy_price_ae`. The buy price is what the next person
pays to enter; the sell price is the side you are on.

The agent skill's portfolio guide documents a `balance_ae_value` field — "current AE value of that
position" — which the current script does not emit. Compute it yourself from `balance` and
`sell_price_ae`, with the caveat in the next section.

## Portfolio history

Backed by `GET /api/accounts/{address}/portfolio/history`, which accepts `startDate`, `endDate`,
`interval`, `convertTo` and `include`. Pass `include=pnl` or `include=pnl-range` to get the PnL
block — the two differ, and the difference matters; see below.

### There are no stored snapshots

"Snapshot" is the response's word, not a description of how it is produced. Nothing is recorded
periodically. The backend takes your `startDate`, `endDate` and `interval`, generates the
timestamps, maps each to a block height, and **reconstructs** holdings at that height by replaying
your own transaction rows. The AE side is a historical `getBalance` against the node.

Two consequences:

- **Every interval returns real data**, not interpolation. There is no underlying sampling frequency to be finer than.
- **The interval you ask for is a floor, not a promise.** The controller raises it based on how long a range you asked for:

  | Range length | Minimum interval |
  | --- | --- |
  | Under 1 month | 1 hour |
  | 1 to 3 months | 4 hours |
  | 3 to 6 months | 1 day |
  | 6 months or more | 1 week |
  | No dates given at all | 1 day |

  The effective interval is `max(your interval, that minimum)`; the default when you pass none is
  86,400 seconds. Ask for 5-minute granularity over a year and you get weekly points, silently.

One piece of real coarseness is worth knowing about: the AE balance lookup floors each block height
to a multiple of 300 key blocks — roughly 15 hours — so snapshots inside the same bucket share one
node call and therefore one AE balance. Token values are exact per snapshot; the liquid-AE line is
stepped. Responses are cached for 10 minutes.

Each snapshot:

| Field | Meaning |
| --- | --- |
| `timestamp` | When the snapshot was taken |
| `total_value_ae` | Everything the account holds, in AE |
| `ae_balance` | Liquid AE |
| `tokens_value_ae` | Value held in token positions |
| `total_value_usd` | The same total, converted |
| `total_pnl` | Present when `include=pnl-range`; see below |

And inside `total_pnl`:

| Field | Meaning |
| --- | --- |
| `percentage` | `gain.ae / invested.ae × 100`, or `0` when nothing was invested |
| `invested` | Cost basis, as `ae` and `usd` — defined differently in the two modes, see below |
| `current_value` | Market value of tokens still held, as `ae` and `usd`. Always cumulative |
| `gain` | The profit figure, as `ae` and `usd` |
| `range` | The `from` and `to` bounds — present **only** under `include=pnl-range` |

### How PnL is actually computed

Cost basis is **average cost, not FIFO**. There is no lot tracking anywhere in the backend: it sums
what you spent and what you bought, and divides.

`include=pnl` — cumulative mode, per token, summed across tokens:

```text
invested      = every AE you ever spent on this token       (all buy + create_community rows)
current_value = tokens still held × current unit price
gain          = proceeds from all your sells + current_value − invested
```

`include=pnl-range` — realised-only mode, per snapshot window:

```text
avg_cost      = (all-time AE spent) / (all-time volume bought)
invested      = avg_cost × volume sold in this window
gain          = sell proceeds in this window − invested
```

Range mode deliberately excludes unrealised gains, so a day only turns green or red when a trade is
actually closed. It is what the app's PnL calendar and sparkline are built on. Cumulative mode is
what you want for "how is this position doing".

A partial sell is therefore attributed at the **all-time average purchase price**, blended across
every buy you ever made in that token — including buys made after the sell, since the average is
taken over your whole history rather than only the part preceding it. That keeps the arithmetic
stable across a reindex, but it means a realised gain reported for a past day can change if you buy
more of the same token later.

Three details that decide whether a number looks right to you:

- **The 0.5% affiliation fee is inside `invested`.** The cost basis comes from the transaction row's `amount`, which is decoded from the sale contract's `Buy` event — its first argument is `total_price`, the fee-inclusive AE you actually paid.
- **Gas is not.** The æternity transaction fee never reaches the contract, so it appears in no row and in no cost basis. Your real cost is slightly higher than `invested` says.
- **`current_unit_price` is the last trade's price, not a live quote.** It is the `buy_price.ae` of the most recent transaction at or before the snapshot height. On a token nobody has traded in a week, your position is valued at last week's price. It is also a *buy*-side unit price, which overstates an exit for the reason in the next section.

The per-token breakdown does exist, on its own endpoint:
`GET /api/accounts/{address}/portfolio/tokens/history` returns the same snapshot shape with a
`tokens_pnl` map keyed by `sale_address`, each entry carrying `current_unit_price`, `percentage`,
`invested`, `current_value` and `gain`. PnL is always included there. It is **not** available on
`/portfolio/history` at any `include` value — that endpoint's own parameter documentation says so.
The payload is large, so use it deliberately.

The `history` command prints the newest and oldest snapshots, the count, the full array, and a
period PnL it computes itself as `latest.total_value_ae − oldest.total_value_ae`. That is a
difference in **valuation**, not a realised result: it moves when prices move, even if you never
traded.

## Your position is worth less than it says

This is the single most important thing on this page. A holdings row prices your balance at the
current spot sell price — one number times another. But selling is an integral: as your tokens burn,
the supply falls and every subsequent token in your own sell fetches less than the one before it.

```text
quoted value  =  balance × sell_price          ← a rectangle
actual exit   =  0.995 × ∫ from (S − balance) to S of buy(x) dx
```

For a token with 10,000,000 tokens in supply:

| Your balance | Share of supply | Quoted value (AE) | Actual sell return (AE) | Shortfall |
| --- | --- | --- | --- | --- |
| 100,000 | 1% | 10.47 | 10.42 | 0.5% |
| 1,000,000 | 10% | 104.74 | 99.26 | 5.2% |
| 5,000,000 | 50% | 523.72 | 388.53 | 25.8% |

A small position is quoted accurately. A position that is a meaningful fraction of the supply is
not — and the discrepancy grows with your share, which is exactly the case where it matters most. If
you hold half a token's supply, your portfolio page is overstating your exit by a quarter.

To compute the honest figure, call `sell_return(balance)` on the sale contract. That is the number
the curve will actually pay, and it costs nothing to ask. See
[Buying & Selling](/tokens/buying-and-selling).

## Why a position can lose value without you doing anything

Three distinct mechanisms, worth separating because they call for different responses:

1. **Other people sell.** The supply falls, the curve slides down, and every remaining holder's position is repriced lower. Nothing about your holding changed; the price function moved beneath it.
2. **Other people buy.** Your position's AE value rises — but your *share* of the supply falls, because new tokens were minted. On a governance stake, that dilution matters even while the value line goes up.
3. **The AE price moves.** `total_value_usd` can fall while `total_value_ae` is flat, or vice versa. Decide which unit you are actually measuring in and be consistent.

There is no mechanism by which a position gains or loses value from time alone. There is no staking
yield, no rebasing and no inflation schedule.

## Realised versus unrealised

Nothing in the portfolio view is realised until you sell, and the 1% spread means a freshly opened
position starts about 1% underwater. The public
[trading leaderboard](https://superhero.com/trends/leaderboard) takes a different cut: it ranks
accounts by `pnl_usd`, `roi_pct`, `aum_usd` and `mdd_pct` (maximum drawdown) alongside `buy_count`,
`sell_count`, `created_tokens_count` and `owned_trends_count`. Those are the numbers to compare
against if you want to know how a strategy is doing relative to other people rather than relative to
its own entry.

## Read more

- [Buying & Selling](/tokens/buying-and-selling) — `sell_return` and the allowance step, for turning a position back into AE
- [Bonding Curves](/tokens/bonding-curve) — the integral behind the shortfall table above
- [Transaction History](/tokens/transactions) — your actual fills, which is where a cost basis has to come from
- [Reading a Token Page](/tokens/token-page) — the same caveat applied to a token's market cap
- [Accounts API](/api-reference/accounts) — the endpoints behind both views, including the leaderboard
- [Trading leaderboard on superhero.com](https://superhero.com/trends/leaderboard) — live realised PnL, ROI and drawdown by account
- [Trending tokens](https://superhero.com/trends/tokens) — where the positions come from

## Open questions

- **Whether `balance_ae_value` was ever served, or is a documentation error in the skill's portfolio guide.** No field of that name appears in the backend's holdings response or in the current script's output. If you are writing against the guide rather than the API, compute it yourself from `balance` and a `sell_return(balance)` call — and mind the rectangle-versus-integral caveat above.
