---
title: "Social"
description: "Posting, commenting, tipping, hashtags and chat on Superhero."
---

# Social

Posts and comments are written to the `Tipping` v3 contract, so they are permanent,
attributable and impossible to delete. Chat is deliberately *not* on-chain: it runs over Nostr
relays for realtime messaging. Know which is which before you publish anything.

## On-chain or not

| Surface | Where it lives | Permanent? |
| --- | --- | --- |
| Posts | `Tipping` v3 contract, one entry per `tip_id` | Yes — no edit, no delete |
| Comments | The same contract, with a `comment:` media prefix | Yes |
| Tips | A plain AE transfer with a payload naming the post | Yes — it is a transaction |
| Hashtags and topics | Derived by the indexer from post content | Follow the posts |
| Trending tags | Pushed in by an external provider, replacing the whole index each time | No |
| Chat messages | Nostr relays, off-chain | No — relay-dependent |
| Notifications | The Superhero API, delivered over a socket and web push | No |
| Moderation state | `CommunityManagement.aes` per community | Yes, but it only gates chat rooms |

## In this section

- [The Feed](/social/feed) — the popular and latest feeds, the exact ranking formula and its
  weights, and how the infinite feed pages.
- [Posting](/social/posting) — the canonical posting reference: the contract call, the limits,
  the content encoding, and both the UI and CLI paths.
- [Delegated & Gasless Posting](/social/delegated-posting) — `post_without_tip_sig` in enough
  detail to build a relayer against it.
- [Comments](/social/comments) — the `comment:` media-prefix convention that turns a post into
  a reply.
- [Tipping](/social/tipping) — where the AE goes, and why tipping is not a contract call.
- [Hashtags & Topics](/social/hashtags-and-topics) — the bridge from the social layer to the
  token layer.
- [Chat](/social/chat) — Nostr-based, off-chain, relay-dependent, and gated by a CSP allowlist.
- [Notifications](/social/notifications) — every notification type, which of them ever reach
  the web bell, and where a click lands.
- [Moderation](/social/moderation) — how a network with no delete button moderates itself, and
  the narrow thing a mute actually does.

## Read more

- [On-chain social](/concepts/on-chain-social) — the design argument for putting content on a
  chain at all.
- [The Tipping contract](/contracts/tipping) — the contract-level reference for the content
  layer.
- [Your first post](/getting-started/first-post) — the shortest path from a funded wallet to a
  published post.
- [Superhero](https://superhero.com/) — the live feed.
