---
title: "Documentation Roadmap"
description: "What is left to verify, built from the open questions the pages themselves declare."
---

# Documentation Roadmap

What is left to **verify**. Every page declares the facts it could not confirm under its own
`## Open questions` heading; this page collects those declarations into a work queue and groups
them by what it would actually take to close each one.

Regenerate the list at any time:

```bash
# Pages with unresolved questions
grep -rl "## Open questions" pages/

# Every open question, one per line, prefixed by its page
python3 - <<'EOF'
import os, re
for root, _, files in os.walk('pages'):
    for f in sorted(files):
        if not f.endswith('.mdx'):
            continue
        p = os.path.join(root, f)
        m = re.search(r'^## Open questions\s*$(.*?)(?=^## |\Z)', open(p).read(), re.M | re.S)
        if not m:
            continue
        for item in re.split(r'\n(?=[-*] )', m.group(1).strip()):
            print(p, '::', ' '.join(item.split()))
EOF
```

## Priority 1 — One session with network access

**Roughly 33 questions** need a live call and nothing else. No address, constant or endpoint on
this site has been confirmed against a running system; every one of them was read out of a
source file instead.

| Call | Closes questions on |
| --- | --- |
| `fee_to()` on the mainnet DEX factory | [Pools](/defi/pools) — whether the protocol-fee switch is on |
| `aex9_extensions()` on a live community token | [AEX standards](/aeternity/aex-standards) |
| `GET /v3/status` on `mdw.wordcraft.fun` and `mainnet.aeternity.io` | [Networks](/aeternity/networks), [Node & middleware](/api-reference/node-and-middleware) |
| One buy and one sell — read `gasUsed` off the transaction | [AE & gas](/aeternity/ae-and-gas), and every page quoting a cost |
| `GET /api/contracts`, `GET /api/factory` | [Deployed addresses](/contracts/addresses) |
| `GET /api/tokens?limit=101` | [API conventions](/api-reference/conventions) |
| Resolve `ADDRESS_LINK_CONTRACT_ADDRESS` on mainnet | [AddressLink](/contracts/profile-registry), [Deployed addresses](/contracts/addresses) |
| Hash the deployed governance bytecode against the registry's index | [Sophia & FATE](/aeternity/sophia-and-fate) |
| Request from the faucet | [Testnet](/getting-started/testnet) — grant amount and graylist duration |

Record the command, not just the conclusion. A verified number with no reproduction step becomes
unverifiable the moment chain state moves.

## Priority 2 — Questions only Superhero can answer

**Roughly 22 questions.** No amount of code reading closes these; someone has to decide or
publish. Listed most consequential first.

| Question | Page |
| --- | --- |
| **Where should a security vulnerability be reported?** There is still no dedicated address, `SECURITY.md`, or bounty in any of the seven repositories read. The wallet's README clause and `CONTACT_EMAIL` are the closest thing that exists. A project holding user funds should publish a contact. | [Contract security](/contracts/security), [Wallet security](/wallets/security) |
| **Have the contracts been audited since the "NOT SECURITY AUDITED" headers were written?** Every one of the ten `.aes` files still carries the banner. | [Contract security](/contracts/security), [Risks](/tokens/risks) |
| Which support and community channels are official? `@superhero_chain` is the only one traceable to a first-party source, and it is an X account, not support. | [Support](/resources/support), [Links](/resources/links) |
| Which countries are excluded from the rewards programme? No geo check exists anywhere in the backend, so it is an edge or manual rule. | [Legal](/resources/legal), [Rewards programme](/rewards/rewards-program) |
| Do the terms cover bonding-curve token sales and DAOs? The published text describes donations, comments and the swap contracts. | [Legal](/resources/legal) |
| Is there a brand licence, and are there logo clear-space and misuse rules? | [Brand](/resources/brand) |
| Why is the AE–ETH bridge disabled, and is `ae-bridge.com` the supported replacement? Its config carries identical contract addresses. | [Bridge](/defi/bridge) |
| Will the DEX `tvl` sort key be fixed to use liquidity rather than all-time volume? | [Explore](/defi/explore) |
| What criteria drive the operator-only `listed` flag? | [Explore](/defi/explore) |
| Is a UV-gated seed-phrase reveal planned for the built-in wallet? | [Built-in wallet](/wallets/built-in-wallet) |
| Is `testnet.api.dev.tokensale.org` a permanent endpoint, and what availability does `mdw.wordcraft.fun` carry? | [Links](/resources/links), [Networks](/aeternity/networks) |

## Priority 3 — Screenshots

Three pages ask for them and no image exists in the repository. Each needs a session with the
live app and an annotation pass. Establish where images live and how they are referenced before
capturing the first one; the site has no convention for this yet.

- [Navigating the app](/getting-started/navigating-the-app) — one per major section
- [Your first post](/getting-started/first-post) — the composer, each control labelled
- [Your first trade](/getting-started/first-trade) — the trade card, every field labelled

## Priority 4 — Defects in the products these pages describe

These are not documentation gaps. They are bugs, found by reading the source, and each is
documented in place so a reader is not misled. They are listed here so they are not lost.

| Defect | Where |
| --- | --- |
| The agent skill's entry thresholds (`> 100000`, `> 50000`, `> 20000`) can never fire — `trending_score` is bounded to `[0,1]`. | [Strategies](/agents/strategies) |
| `AffiliationTreasury.record_sale_transaction` is unpermissioned and accepts a caller-chosen value that advances the anti-sybil counter. | [AffiliationTreasury](/contracts/affiliation-treasury) |
| `get_sell_return_precision()` returns the percentage constant, so an off-chain caller deriving the ratio gets 1.0 instead of 0.995. | [Bonding curve contract](/contracts/bonding-curve) |
| The in-app bonding-curve graph plots an offset 1000× the deployed constant, and its explainer describes a quadratic curve. | [Bonding curves](/tokens/bonding-curve) |
| The DAO vote-type selector is not wired to the submitted subject — every proposal opens as a payout. | [Proposals](/dao/proposals) |
| `token-created` and `token-transaction` are listened for but never emitted. | [WebSocket](/api-reference/websocket) |
| The DEX `tvl` key sorts by all-time volume; `valueUsd` is never assigned, so Pool's Total Value always reads 0. | [Explore](/defi/explore), [Pools](/defi/pools) |
| The WebAuthn PRF challenge is not bound to the payload hash, contrary to what `inline-signer.ts` requires of itself. | [Built-in wallet](/wallets/built-in-wallet) |
| Superhero maps `ok_` to an æScan route that does not exist. | [Explorers](/aeternity/explorers) |
| Governance `revoke_vote` is ungated, so a closed poll's result can still shrink. | [Voting](/governance/voting) |
| The agent skill's governance guide computes close heights at 3s per key-block; they are ~3 minutes. | [Create a poll](/governance/create-a-poll) |
| `superhero-invite.mjs` initialises the treasury with the factory ACI, so `register_invitation_code` is not on the object. | [CLI reference](/agents/cli-reference) |

## Priority 5 — Keeping it true

This site asserts a great many specific things about code that will change. Worth doing
periodically:

- Re-run `python3 scripts/lint-mdx.py` — it gates frontmatter, MDX hazards, internal links and
  `_meta.json` drift, and exits non-zero on any finding.
- Re-check the twelve whitepaper-versus-code divergences on [Whitepaper](/resources/whitepaper).
  If the whitepaper is revised, that table is the first thing to update.
- Re-read `src/configs/constants.ts` in `superhero-api` when a ranking or scoring page looks
  stale. The backend's own `docs/` markdown has already drifted from it twice.
